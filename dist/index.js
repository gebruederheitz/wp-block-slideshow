import { register, attributes, edit, save } from './editor';
import {
    GlideSlideshowFactory,
    GlideSlideshow,
    defaults,
    imageGallery,
    prevIcon,
    nextIcon,
} from './frontend';

export {
    GlideSlideshowFactory,
    GlideSlideshow,
    defaults,
    imageGallery,
    prevIcon,
    nextIcon,
    register,
    attributes,
    edit,
    save,
};
